// changes Isaac - Miercoles 30 12:30 am

// Cambié el nombre del componente MenuList a Categories

// Modifiqué categories y le añadí la imagen pero no consigo que funcione con la ruta somple de public

// Le añadí unos estilos, no sé como hacerlo más bonito con el import del layour del scss, si me lo puedes corregir luego mans, thanks

// Cambie MenuPage a Categories

// Cambie fleje en el app.jsx que hice un lio con las rutas, elimine menu, por ahora, ya te explicaré

// Cree unos estilos para categories

// Cree el component category {No muy estilizado}

// La página category tiene el un fallo, y es que debido al fixed del tabs bar, se clipea con la última categoría

// Como estaba haciendo padding top para todo, me cree un componente llamado separador, es un simple div

// Edité la pagina orders

// Cree el componente order {Falta detallarlo}

// Offers pasa a ser Offer, y le hice un remake, digamos que es el plato del día básicamente

// Cree la página products, como la muestra de lo escogido

// Cree el componente de product

// En el componente product hay que añadir una variante de color para el button, como el ejemplo de las comillas francesas 